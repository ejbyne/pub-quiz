version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - nvm use $VERSION_NODE_12
        - cd web-app
        - yarn install --frozen-lockfile
    build:
      commands:
        - yarn build
#    test:
#      commands:
#        - yarn test
  artifacts:
    baseDirectory: web-app/build
    files:
      - '**/*'
